
var fs = require('fs');

// Read and eval library
var filedata = fs.readFileSync('./node_modules/data-stream/DataStream.js/DataStream.js','utf8');
eval(filedata);

/* The DataStream.js file defines a class 'DataStream' which is all we want to export */
module.exports = {
  DataStream: DataStream
};
